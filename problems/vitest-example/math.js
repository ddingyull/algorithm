/**
 * 수학 관련 유틸리티 함수들
 */

/**
 * 두 수의 합을 반환
 */
export function sum(a, b) {
  return a + b;
}

/**
 * 두 수의 곱을 반환
 */
export function multiply(a, b) {
  return a * b;
}

/**
 * 숫자가 짝수인지 확인
 */
export function isEven(n) {
  return n % 2 === 0;
}